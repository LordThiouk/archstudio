node_modules/
.next/
out/
dist/
data/*.db
data/*.db-shm
data/*.db-wal
.env.local
# docker compose reads .env, and the Docker section of the README points API
# keys at it — it must never be committable.
.env
.DS_Store
*.log
next-env.d.ts
tsconfig.tsbuildinfo

# Third-party reference material kept locally and never redistributed.
# The brackets are escaped: unescaped, `[External]` is a character class and
# the rule would silently match nothing.
\[External\]*
